// Design Team Intelligence System — Prisma schema
// Full 21-entity schema implemented in Phase 1 (Prompt 1).
// This is the Phase 0 stub — datasource + generator only.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}
